# Feature Ledger — Superset Adapter

| feature_id | surface/path/component | user_story | expected_behavior | current_status | evidence | issue_type | fix_status | retest_receipt |
|---|---|---|---|---|---|---|---|---|
| SA-001 | `relayproof.adapters.superset.SupersetConfig` | Operator supplies endpoint/token/ids without committing or logging secrets. | Typed config, explicitly selected manifest, redacted config/snapshot/error representations. | Implemented; local contract GREEN. | Strict bounded `0600` non-symlink manifest tests, duplicate-key rejection, and token/error redaction. | feature | fixed | `test_config_manifest_is_strict_bounded_and_secret_is_redacted`; `test_unsafe_manifests_fail_without_raw_content`; `test_timeout_and_error_never_include_secret_or_terminal_content` |
| SA-002 | `SupersetAdapter.snapshot` / `terminal.snapshot` | Read current terminal revision/text from the actual host tRPC contract. | Loopback-only GET to `<endpoint>/terminal.snapshot`; SuperJSON envelopes; bounded no-proxy/no-redirect transport. | Implemented; fake-server contract and live read-only smoke GREEN. | Exact GET contract, wrong-target, oversized-response, proxy, redirect, and live revision/dimensions readback. | feature | fixed-live | `test_snapshot_uses_exact_real_get_contract`; `test_wrong_snapshot_target_is_rejected`; `test_oversized_response_is_rejected_before_json_parse` |
| SA-003 | `SupersetAdapter.dispatch` / `terminal.send` | Send one idempotent command to a concrete target. | POST with expected revision, stable client token, `submit=true`, empty-prompt guard, and repeats disabled; zero-network dry-run unless confirmed. | Implemented; adversarial fake-server contract GREEN; no live send performed by policy. | Exact POST envelope, local token mutation rejection, stale/duplicate/wrong-target and ambiguous-transport tests. | feature | fixed | `test_send_is_dry_run_then_exact_confirmed_post`; `test_token_reuse_with_mutated_text_or_revision_fails_locally`; `test_ambiguous_post_transport_failure_is_not_retried` |
| SA-004 | `SupersetAdapter.await_canary` / evidence mapping | Distinguish dispatch from proven acceptance. | Structured marker; absent from prompt/baseline; strictly newer revision; monotonic timeout and max-attempt bound. | Implemented; false-GREEN adversarial contracts GREEN. | Prompt-echo, baseline, revision-only, revision-reset, max-attempt and timeout tests. | feature | fixed | `test_canary_rejects_literal_prompt_and_baseline_before_polling`; `test_canary_requires_structured_marker_and_advanced_revision`; `test_revision_movement_without_canary_times_out_red_with_max_attempts` |
| SA-005 | `relayproof superset status|send` | Inspect status safely and run send/canary only with explicit confirmation. | Status is read-only and omits terminal text; send is zero-network dry-run unless `--confirm-send`; output omits token/text. | Implemented; CLI tests GREEN. | Mocked status readback and unreachable-endpoint dry-run test. | feature | fixed | `test_superset_status_is_read_only_and_does_not_print_terminal_text`; `test_superset_send_defaults_to_zero_network_dry_run` |
| SA-006 | Package/build/CI | Collaborators clone and run tests/build. | Full suite, Ruff, compile, doctor, wheel, and GitHub Actions. | Local gates GREEN; adapter push check pending. | 35/35 unittest PASS; Ruff PASS; compileall PASS; gitleaks PASS; doctor expected RED fixture; wheel built. | verification | fixed-local | `python3 -m unittest discover -s tests -v`; `ruff check src tests`; `python3 -m compileall -q src tests`; `python3 -m build --wheel` |

## Claim boundary

- Adapter implementation and fake-server contracts: **GREEN**.
- Live read-only Superset `terminal.snapshot` smoke: **GREEN** (revision/dimensions/target readback; no text/token output).
- Live `terminal.send`: **not run by policy**.
- Hosted CI: adapter push receipt pending.
