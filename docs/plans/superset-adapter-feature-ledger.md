# Feature Ledger — Superset Adapter

| feature_id | surface/path/component | user_story | expected_behavior | current_status | evidence | issue_type | fix_status | retest_receipt |
|---|---|---|---|---|---|---|---|---|
| SA-001 | `yapitalism.adapters.superset.SupersetConfig` | Operator supplies endpoint/token/ids without committing or logging secrets. | Typed config, explicitly selected manifest, redacted config/snapshot/error representations. | Implemented; local contract GREEN. | Strict bounded `0600` non-symlink manifest tests, duplicate-key rejection, and token/error redaction. | feature | fixed | `test_config_manifest_is_strict_bounded_and_secret_is_redacted`; `test_unsafe_manifests_fail_without_raw_content`; `test_timeout_and_error_never_include_secret_or_terminal_content` |
| SA-002 | `SupersetAdapter.snapshot` / `terminal.snapshot` | Read current terminal revision/text from the actual host tRPC contract. | Loopback-only GET to `<endpoint>/terminal.snapshot`; SuperJSON envelopes; bounded no-proxy/no-redirect transport. | Implemented; fake-server contract and live read-only smoke GREEN. | Exact GET contract, wrong-target, oversized-response, proxy, redirect, and live revision/dimensions readback. | feature | fixed-live | `test_snapshot_uses_exact_real_get_contract`; `test_wrong_snapshot_target_is_rejected`; `test_oversized_response_is_rejected_before_json_parse` |
| SA-003 | `SupersetAdapter.dispatch` / `terminal.send` | Send one idempotent command to a concrete target. | POST with expected revision, explicit stable client token, `submit=true`, empty-prompt guard, repeats disabled, and 64 KiB UTF-8 bound; zero-network dry-run unless confirmed. | Implemented; adversarial fake-server contract GREEN; no live send performed by policy. | Exact POST envelope, identical-token replay, token mutation, contradictory response, size-bound, stale/duplicate/wrong-target and ambiguous-transport tests. | feature | fixed | `test_identical_token_replay_is_sent_unchanged_and_parsed_as_duplicate`; `test_contradictory_injected_envelopes_fail_closed`; `test_dispatch_text_has_utf8_byte_bound_before_network`; `test_ambiguous_post_transport_failure_is_not_retried` |
| SA-004 | `SupersetAdapter.await_canary` / evidence mapping | Distinguish dispatch from proven acceptance. | Structured line-wrap-safe marker; normalized marker absent from prompt/baseline; strictly newer revision; monotonic timeout and max-attempt bound. | Implemented; false-GREEN adversarial contracts GREEN. | Literal/ANSI-split prompt echo, baseline, wrapped marker, revision-only, revision-reset, max-attempt and timeout tests. | feature | fixed | `test_canary_rejects_literal_prompt_and_baseline_before_polling`; `test_canary_requires_structured_marker_and_advanced_revision`; `test_revision_movement_without_canary_times_out_red_with_max_attempts` |
| SA-005 | `yapitalism superset status|send` | Inspect status safely and run send/canary only with explicit confirmation. | Status is read-only and omits terminal text; send is zero-network dry-run unless `--confirm-send`; output omits token/text. | Implemented; CLI tests GREEN. | Mocked status readback and unreachable-endpoint dry-run test. | feature | fixed | `test_superset_status_is_read_only_and_does_not_print_terminal_text`; `test_superset_send_defaults_to_zero_network_dry_run` |
| SA-006 | Package/build/CI | Collaborators clone and run tests/build. | Full suite, Ruff, compile, doctor, wheel, and GitHub Actions. | Local gates GREEN; review-fix push check pending. | 40/40 unittest PASS; Ruff PASS; compileall PASS; gitleaks PASS; doctor expected RED fixture; wheel built. | verification | fixed-local | `python3 -m unittest discover -s tests -v`; `ruff check src tests`; `python3 -m compileall -q src tests`; `python3 -m build --wheel` |
| SA-007 | `ConfirmationClaimStore` / confirmed CLI send | A reviewed dry run remains bound to the exact command across CLI processes. | Owner-only expiring claim binds token, command hash, revision, and terminal; confirmed send consumes it before mutation. | Implemented; local contracts GREEN. | Cross-process reload, mutation rejection, expiry, re-issue, consume, and missing-claim mutation-blocking tests. | security | fixed-local | `test_claim_survives_process_boundary_and_is_single_use`; `test_confirm_without_claim_blocks_mutation`; `test_full_dry_run_confirm_flow_persists_dispatch_and_accept` |
| SA-008 | JSONL ledger / `receipt show` | Operator can replay persisted dispatch and acceptance evidence without raw command text. | Dry run, confirmed dispatch, ambiguous transport, and canary events persist; unknown receipt IDs fail explicitly. | Implemented; local contracts GREEN. | Ledger round-trip, unknown receipt, ambiguous no-retry, permission, and non-leakage tests. | feature | fixed-local | `test_superset_dry_run_persists_receipt_and_confirmation_claim`; `test_ambiguous_confirm_is_persisted_without_retry`; `test_receipt_show_rejects_unknown_command` |
| SA-009 | Structured canary matcher | Ordinary agent acknowledgement text does not produce a false RED. | ANSI and line wrapping remain supported while spaces around a canary preserve token boundaries. | Implemented; regression GREEN. | `OK <canary>`, `ACK: <canary>`, and `<canary> DONE` regression cases. | bug | fixed-local | `test_structured_canary_accepts_common_acknowledgement_boundaries` |
| SA-010 | Receipt projection / JSONL ledger ordering | A retry result does not conflict with stale failure history. | Ledger-assigned sequence is ordering authority; explicit supersession removes stale events from status and summary without deleting history. | Implemented; local contracts GREEN. | Out-of-order record and failed-then-superseded-success projection tests. | correctness | fixed-local | `test_superseded_failure_does_not_poison_status_or_summary`; `test_sequence_not_wall_clock_controls_latest_event` |
| SA-011 | JSONL ledger integrity / `ledger verify` / `ledger migrate` | Operator can detect local corruption, reject duplicate-ID mutation, and upgrade without rewriting history. | Locked append, contiguous sequence, canonical hash chain, identical duplicate idempotency, mutation rejection, `fsync`, explicit verification, pre-network compatibility check, and non-destructive legacy copy. | Implemented; local contracts GREEN. | Sequence/link assertions, duplicate mutation rejection, tamper detection, CLI verification, mutation-blocking preflight, and source-preserving migration. | security | fixed-local | `test_append_assigns_monotonic_sequence_and_hash_chain`; `test_duplicate_event_id_is_idempotent_but_mutation_is_rejected`; `test_verify_detects_tampering`; `test_ledger_verify_reports_chain_head_and_tampering`; `test_legacy_ledger_blocks_send_before_adapter_activity`; `test_legacy_ledger_migration_is_non_destructive_and_verified` |
| SA-012 | `EvidenceEvent` identity envelope | Operator can distinguish attribution, source, target, session, command, and transport delivery without turning routing metadata into proof. | Optional bounded non-empty identity dimensions persist independently and never change leg state/provenance authority. | Implemented; local contracts GREEN. | Identity persistence, empty-field rejection, and pending-state projection tests. | correctness | fixed-local | `test_identity_dimensions_persist_without_changing_proof_authority`; `test_empty_or_oversized_identity_dimension_is_rejected` |
| SA-013 | `ledger manifest` / sole-authority contract | Operator can compare stores and projections without exposing receipt payloads or enabling dual authority. | Manifest derives from one verified ledger snapshot and emits only versions, validity, sequence range, counts, and chain head; JSONL remains sole writable authority. | Implemented; local contracts GREEN. | Manifest derivation and CLI privacy tests; ADR-0005 cutover state machine. | architecture | fixed-local | `test_manifest_is_derived_from_verified_ledger`; `test_ledger_manifest_reports_authority_without_event_payloads` |

## Claim boundary

- Adapter implementation and fake-server contracts: **GREEN**.
- Live read-only Superset `terminal.snapshot` smoke: **GREEN** (revision/dimensions/target readback; no text/token output).
- Live `terminal.send`: **not run by policy**.
- Hosted CI: adapter push receipt pending.
